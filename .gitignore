# ── Dependencies ─────────────────────────────────────────────────────────────
node_modules/

# ── Expo ─────────────────────────────────────────────────────────────────────
.expo/
dist/
web-build/
*.orig.*

# ── EAS / Native builds ───────────────────────────────────────────────────────
.kotlin/
/ios/
/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.keystore

# ── Metro bundler ─────────────────────────────────────────────────────────────
.metro-health-check*

# ── Environment & secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.development
.env.production
.env*.local

# ── Logs ─────────────────────────────────────────────────────────────────────
npm-debug.*
yarn-debug.*
yarn-error.*
*.log
logs/

# ── OS files ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor & IDE ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── TypeScript ────────────────────────────────────────────────────────────────
*.tsbuildinfo

# ── Package manager locks (keep only one) ────────────────────────────────────
# Keeping package-lock.json, ignoring yarn/pnpm locks
yarn.lock
pnpm-lock.yaml

# ── Temporary files ───────────────────────────────────────────────────────────
*.tmp
*.temp
.cache/
